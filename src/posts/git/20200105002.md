---
category: git
title: git 오류 및 해결방법
summary: "A branch named 'gh-pages' already exists."
date: "2020-01-05 12:00"
tag: ["already exists", ".cache", "rm -rf"]
author: ""
---

### 오류 발생 상황 ( package.json의 yarn deploy 실행 )

```json
package.json
"scripts": {
    ...
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
}

```

```shell
yarn deploy
```

### 발생오류 => A branch named 'gh-pages' already exists.

```shell wrap
ProcessError: fatal: A branch named 'gh-pages' already exists.

    at ChildProcess.<anonymous> (D:\workspace\reactJs\minsblog\node_modules\gh-pages\lib\git.js:42:16)
    at ChildProcess.emit (events.js:210:5)
    at maybeClose (internal/child_process.js:1021:16)
    at Process.ChildProcess._handle.onexit (internal/child_process.js:283:5) {
  code: 128,
  message: "fatal: A branch named 'gh-pages' already exists.\n",
  name: 'ProcessError'
}
```

### 원인

> Gthub의 기존 repositoroy를 삭제 및 생성을 몇번하면서 프로젝트의 branch cache가 남아있어서 발생한 문제

### 해결방법

```shell
rm -rf node_modules/gh-pages/.cache
```
